apiVersion: v2
name: jenkins-deployment
description: Jenkins CI/CD Server Deployment - Umbrella Chart
type: application
version: 1.0.0
appVersion: "2.440"

# Dependencies - Official Jenkins Helm Chart
dependencies:
  - name: jenkins
    version: "5.1.27"
    repository: https://charts.jenkins.io
    condition: jenkins.enabled
