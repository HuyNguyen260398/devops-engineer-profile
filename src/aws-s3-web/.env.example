# Cognito config for the blog app (blogs.nghuy.link).
# Populated automatically in CI from Terraform outputs; set locally to develop
# the /login, /admin, and /admin/editor routes against a deployed backend.
NEXT_PUBLIC_USER_POOL_ID=
NEXT_PUBLIC_USER_POOL_CLIENT_ID=
